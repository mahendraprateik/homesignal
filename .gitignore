# Environment & secrets
.env
.env.*

# Python virtual environment
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Data — raw source files (large, not needed at runtime)
data/raw/

# Note: data/homesignal.db and data/chroma_db/ are tracked in git
# because Streamlit Community Cloud has no persistent disk.
# Run the pipeline locally, then commit these files before deploying.

# OMC runtime state (plans are committed, runtime state is not)
.omc/state/
.omc/project-memory.json

# Streamlit cache
.streamlit/cache/

# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
